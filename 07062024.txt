SRC FOLDER (not whole),
SERVER CREATION,
NODEMON SETUP,
Dynamic port,
Little bit http module,
QnA,
URL parts => namespace, linking phrase, end-point


Second lecture => Same day
Folder structure || flowchart || github repo for node js folder structure || nodejs development flow

HW: server creation using express,
	nodemon setup,
	server js time functions run
	nodejs words || terminology